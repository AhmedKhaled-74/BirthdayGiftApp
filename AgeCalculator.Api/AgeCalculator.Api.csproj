<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>AgeCalculator.Api</RootNamespace>
    <SpaRoot>..\age-calculator-web\</SpaRoot>
    <SpaDist>..\age-calculator-web\dist\age-calculator-web\browser\</SpaDist>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Identity.EntityFrameworkCore" Version="10.0.12" />
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.12" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.12">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.3" />
  </ItemGroup>

  <!-- Production SPA build: `dotnet publish` builds Angular and copies it
       into the publish wwwroot. Docker prebuilds the SPA in a node stage
       (copied to wwwroot before publish) and passes /p:SkipSpaBuild=true.
       Hosts with the SPA dist committed (e.g. MonsterASP.NET, where npm is
       blocked by group policy) skip npm and copy the committed dist. -->
  <Target Name="BuildAngular" AfterTargets="Publish" Condition="'$(SkipSpaBuild)' != 'true'">
    <Exec Command="npm ci" WorkingDirectory="$(SpaRoot)" Condition="!Exists('$(SpaDist)index.html')" ContinueOnError="WarnAndContinue" />
    <Exec Command="npm run build" WorkingDirectory="$(SpaRoot)" Condition="!Exists('$(SpaDist)index.html')" ContinueOnError="WarnAndContinue" />
    <ItemGroup>
      <DistFiles Include="$(SpaDist)**" />
    </ItemGroup>
    <Copy SourceFiles="@(DistFiles)" DestinationFiles="@(DistFiles->'$(PublishDir)wwwroot\%(RecursiveDir)%(Filename)%(Extension)')" SkipUnchangedFiles="true" />
    <Warning Condition="!Exists('$(SpaDist)index.html')" Text="Angular dist not found at $(SpaDist); publish output has no frontend. Run 'npm run build' in $(SpaRoot) and commit the dist, or ensure npm is available." />
  </Target>

</Project>