# Angular SPA and ASP.NET Core API boundary

The application uses an Angular 20 single-page application and a separate ASP.NET Core Web API. Angular owns presentation and sends the browser time zone; the API owns authentication, authorization, Profile persistence, and the authoritative Age Summary and Birthday Celebration result, keeping browser presentation separate from business rules.
