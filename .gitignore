# .NET build output
**/bin/
**/obj/
*.user

# Visual Studio
.vs/

# JetBrains
.idea/

# Logs
*.log

# Tooling
*.binlog

# Node / Angular (dependencies never committed; built SPA dist IS committed so
# hosts without npm, e.g. MonsterASP.NET, can publish without running npm)
node_modules/

# Published SPA bundle (built on `dotnet publish` / Docker, never committed)
AgeCalculator.Api/wwwroot/